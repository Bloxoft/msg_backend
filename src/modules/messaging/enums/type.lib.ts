export enum RoomType {
    P2P = 'p2p',
    GROUP = 'group'
}

export enum MSGDeleteType {
    PERSONAL = 'personal',
    EVERYONE = 'everyone'
}

export enum MessageType {
    NORMAL = 'normal',
    NOTIFICATION = 'notification',
    LOCATION = 'location',
    CONTACT = 'contact',
    POLL = 'poll',
    DOCUMENT = 'document'
}

export enum PushNotificationType {
    MESSAGE = 'message',
}