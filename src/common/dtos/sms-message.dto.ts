/**
 * Represents an SMS message with a text and a recipient's phone number.
 */
export class SmsMessage {
    /**
     * The content of the SMS message.
     */
    message: string;

    /**
     * The recipient's phone number.
     */
    phoneNumber: string;
}
